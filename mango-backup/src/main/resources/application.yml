server:
  port: 8002

spring:
  application:
    name: mango-backup
  autoconfigure:
    exclude:
      - org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration

mango:
  backup:
    datasource:
      host: localhost
      userName: root
      password: root
      database: mango

